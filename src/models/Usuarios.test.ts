import { Usuarios } from "./Usuarios"

describe ('Testa a segurança do trecho de senha do usuário', () => {
    test('cria login e senha e verifica se os dados inseridos corretamente validam o login', () => {
        const nome = new Usuarios()
        expect(nome.nome).toBe()
    })
})